# 线程交接清单

交接文件使用 `<lane>-generation-<n>.md` 命名，创建后不可覆盖。

每份清单必须包含：

- 来源线程和继任线程 ID
- OpenSpec change 和任务 ID
- worktree、branch 和完整 HEAD commit
- 已完成、进行中和待办任务
- 当前公共契约和不可变决策
- 修改文件和文件所有权
- 已运行测试、结果和未运行原因
- 阻塞项、残余风险和推荐下一步
- 可观测压缩次数、事件指纹和健康审计结果

继任任务必须先验证 HEAD 和 OpenSpec 状态，再开始修改文件。
