## 1. Persistence foundation and configuration

- [x] 1.1 `[lane=library; owner=main agent; depends=planning artifacts]` Add the Library Vitest harness and implement typed errors, contained path helpers, exclusive versioned naming, tokenized bounded file locks, atomic UTF-8 JSON replacement/backups, transaction-journal primitives, and injectable POSIX/Windows permission enforcement. Allowed files: `packages/library/package.json`, `packages/library/vitest.config.ts`, `packages/library/src/errors.ts`, `packages/library/src/fs/**`, and focused `packages/library/test/fs/**`. Forbidden: config/business models, upload/gallery/ZIP behavior, shared contracts, Foundation, root files, dependencies/lockfile, other lanes. Verification before commit: fresh apply instructions; `pnpm --filter @routego-image/library typecheck`; focused Library tests for concurrency, timeout, stale-lock race, crash/recovery, Unicode paths, versioned collision, POSIX mode and mocked SID/ACL failure; `pnpm --filter @routego-image/library build`; `pnpm safety`; `git diff --check`; allowed-file scope review. Implementation commits: `a63dd70e49ce7570bebbfdfae236fc46d91d1cfd`, `f2970e3c7303c4074e2b52c12bf5c770cf10f8df`.

- [x] 1.2 `[lane=library; owner=main agent; depends=1.1]` Implement versioned non-sensitive config and separate credential stores, profile read/upsert/remove/set-active, cached model/capability persistence, defaults and `updateSettings`, strict output-directory canonicalization/legacy/symlink/ownership/creatability validation, redacted displays, and recovery. Allowed files: `packages/library/src/config/**` and focused `packages/library/test/config/**`. Forbidden: provider network calls, upload/gallery/ZIP code, full-path results/logs, real credentials, shared/root/other-lane files. Verification before commit: fresh apply instructions; focused tests for corrupt/future versions, all API-key mutations, Windows/POSIX permission failure, endpoint non-derivation, unknown/supported/unsupported/degraded evidence persistence, transient-state preservation, defaults, all output-directory mutations and unsafe path cases; Library typecheck/test/build; safety; diff check; scope review. Implementation commits: `c7f0ea6ab32c52f84ba2c74f92dd765e4810d534`, `968a6924e4dab7aee5b3aaec903a8cc0c2be3d52`.

## 2. Upload resource ownership

- [x] 2.1 `[lane=library; owner=main agent; depends=1.1]` Implement the durable upload registry and contained staging API behind `StudioUploadService`: reserve policy, bounded binary writing, uploaded/finalized/failed state, MIME/magic/size/SHA-256/dimension checks, status, lazy expiry, discard, reusable image resolution, single-consume ZIP transition, cleanup, and internal resolver metadata without provider execution. Allowed files: `packages/library/src/image/**`, `packages/library/src/upload/**`, and focused `packages/library/test/upload/**`. Forbidden: HTTP/session/origin adapters, Studio creation/provider execution, ZIP archive parsing, gallery index, shared/root/other-lane files. Verification before commit: fresh apply instructions; every upload purpose and state; oversize streaming stop; invalid type/corrupt header/checksum; expiry/discard/not-found; reusable image; ZIP consume only after explicit success; contained path/redaction; interrupted staging recovery; Library typecheck/test/build; safety; diff check; scope review. Implementation commit: `eed07e3918547dced2035a4404a3b79a9e58c8d5`.

## 3. Durable gallery and Library services

- [x] 3.1 `[lane=library; owner=main agent; depends=1.1,2.1]` Implement versioned Library index models/parsers/repository, logical assets and SHA-256 blob deduplication, validated PNG/JPEG/WebP ingestion, exclusive date-partitioned placement, project-copy versioning, transaction recovery, and stable asset/artifact/upload internal resolution. Allowed files: `packages/library/src/gallery/model.ts`, `packages/library/src/gallery/index-store.ts`, `packages/library/src/gallery/assets.ts`, `packages/library/src/gallery/resolver.ts`, and focused `packages/library/test/gallery/assets*.test.ts`. Forbidden: search/folders/recycle/ZIP/adapters, provider execution, shared/root/other-lane files. Verification before commit: fresh apply instructions; missing/corrupt/future index; supported/invalid/truncated images; metadata mismatch; duplicate logical assets sharing one blob; concurrent ingestion/name collisions; project copy no-overwrite; crash before/after commit; locator resolution; legacy/traversal rejection; Library typecheck/test/build; safety; diff check; scope review. Implementation commit: `eb111efbcdd2fb20317bd78725512edd10458ba7`.

- [x] 3.2 `[lane=library; owner=main agent; depends=3.1]` Implement public and path-free Studio search with deterministic keyset cursors, folder create/rename/list/reorder/assign/remove, complete details and relationships, allowed actions, and short-lived thumbnail/preview/original/ZIP browser-resource registrations. Allowed files: `packages/library/src/gallery/query.ts`, `packages/library/src/gallery/folders.ts`, `packages/library/src/gallery/resources.ts`, `packages/library/src/gallery/read-service.ts`, and focused `packages/library/test/gallery/query*.test.ts`, `folders*.test.ts`, `resources*.test.ts`. Forbidden: destructive mutations, ZIP codec/import/export, config/upload registry changes, shared/root/other-lane files. Verification before commit: fresh apply instructions; every filter/sort/cursor; malformed cursor; public path versus Studio path-free projections; non-empty pages; ID alignment across search/detail/resource; all relationship roles; folder normalization/many-to-many/reorder conflict; resource expiry/no path leak; Library typecheck/test/build; safety; diff check; scope review. Implementation commit: `1dbdf40dd939e658deccf47862fe65c2a5f72e2d`.

- [x] 3.3 `[lane=library; owner=main agent; depends=3.1,3.2]` Implement soft delete, 30-day purge metadata, restore, permanent-delete preflight/execution, per-item partial outcomes, unreferenced blob cleanup/recovery, public `routego_manage_library` compatibility mapping, and Studio Library mutation service behavior excluding ZIP internals. Allowed files: `packages/library/src/gallery/mutations.ts`, `packages/library/src/gallery/service.ts`, and focused `packages/library/test/gallery/mutations*.test.ts`, `service*.test.ts`. Forbidden: ZIP codec/portability implementation, config/upload code changes, shared/root/other-lane files. Verification before commit: fresh apply instructions; soft delete visibility/restore previous status; confirmation/preflight expiry/staleness; mixed partial results; shared blob retention/final cleanup; public affected-ID honesty and warnings; unknown file preservation; legacy protection; Library typecheck/test/build; safety; diff check; scope review. Implementation commit: `a7d1c2f0cb3661001d35e388a1786cbab874a426`.

## 4. ZIP portability

- [x] 4.1 `[lane=library; owner=main agent; depends=1.1]` Implement the dependency-free CRC-32 and bounded ZIP reader/writer subset with UTF-8 names, store/deflate, central-directory-first parsing, exclusive publication, and rejection of encryption, multi-disk, unsupported methods, duplicates, unsafe paths/attributes, limit/ratio violations, and inconsistent CRC/headers. Allowed files: `packages/library/src/zip/crc32.ts`, `packages/library/src/zip/codec.ts`, and focused `packages/library/test/zip/codec*.test.ts`. Forbidden: gallery/index/upload integration, manifest business records, dependencies/lockfile, shared/root/other-lane files. Verification before commit: fresh apply instructions; round-trip store/deflate/Unicode; traversal/drive/UNC/backslash/NUL/duplicate/symlink; encrypted/unsupported/multi-disk; corrupt central/local headers; CRC failure; entry/archive/total/ratio limits; interrupted exclusive output; Library typecheck/test/build; safety; diff check; scope review. Implementation commit: `ed9bd659e855158f5d5f757944fc48d07132f133`.

- [x] 4.2 `[lane=library; owner=main agent; depends=2.1,3.1,3.2,3.3,4.1]` Implement the versioned credential-free portability manifest, selected-asset ZIP export, protected ZIP resource registration, finalized upload-backed import, full prevalidation, SHA/MIME/dimension checks, blob reuse, exact-record skip, consistent ID/relationship/folder remapping, journaled atomic/partial integration, and ZIP single consumption after commit. Allowed files: `packages/library/src/zip/manifest.ts`, `packages/library/src/zip/portability.ts`, and focused `packages/library/test/zip/portability*.test.ts`. Existing upload/gallery modules are read-only dependencies. Forbidden: credentials/settings export with secrets, legacy migration, provider/HTTP/UI behavior, shared/root/other-lane files. Verification before commit: fresh apply instructions; credential/path/token absence; selected relationship/folder closure; missing/duplicate entry; CRC/SHA/MIME/dimension mismatch; blob reuse; ID remap; exact skip; cross-platform Unicode; crash recovery; item partial outcomes; ZIP consumed only after commit; protected export descriptor; Library typecheck/test/build; safety; diff check; scope review. Implementation commit: `ae0be0b8f197f8339c787ad1ade8b7363e217e83`.

## 5. Package composition and final delivery

- [x] 5.1 `[lane=library; owner=main agent; depends=1.2,2.1,3.1,3.2,3.3,4.1,4.2]` Compose and export the configuration, upload, gallery, portability, service, and resolver APIs from `@routego-image/library`; add cross-module contract tests proving `StudioUploadService`, persistence-owned settings operations, `StudioLibraryService`, public search/manage storage behavior, stable locator ownership, and no provider execution. Allowed files: `packages/library/src/index.ts`, `packages/library/src/service.ts`, `packages/library/test/conformance/**`, and minimum package-local config required for tests/exports. Forbidden: shared contracts/Foundation/mock/root/lockfile, Creation/Studio, transports, provider calls, manifest/release files. Verification before commit: fresh apply instructions; all Library tests; Library typecheck/build; root `pnpm safety`, `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm check:exports`; seven-public-operation assertion; runtime/native dependency audit; package export smoke; diff check; scope review. Implementation commits: `8feb9dc2d4f53336d1190fc069b2774ddf4c241b`, `634712d5c4233f1a58ff1a33d94420fcb3504367`. Independent review correction: public ZIP export now writes every byte from legal partial `FileHandle.write` results, validates the canonical destination through existing ancestors before publication, rejects symlink/junction traversal into protected legacy roots, preserves exclusive no-overwrite behavior, and has focused decodable/importable and protected-root regressions.

- [x] 5.2 `[lane=library; owner=main agent; depends=5.1]` Run the complete Library conformance gate in a clean verification copy: frozen install, strict OpenSpec, safety, typecheck, build, all tests, package exports, no native runtime dependency, Windows/POSIX behavior coverage, secret/image/path repository audit, diff scope, planning-consistency audit, and final Git cleanliness. If failure requires code, return to its owning task and repeat verification/commit before completion. Allowed files before final delivery: this change's `tasks.md` task-state/commit records and the Library thread delivery state only after all implementation commits. Verification: fresh apply instructions; `pnpm install --frozen-lockfile`; `openspec validate --all --strict --no-interactive`; `pnpm safety`; `pnpm typecheck`; `pnpm build`; `pnpm test`; `pnpm check:exports`; dependency/native audit; `git diff --check`; scope review against `f6e1b309213bb87d741199377c2bebdaa9cbcad0`; clean `git status`; re-read proposal/design/specs/tasks/PD-002/PD-003/PD-004/thread state. Corrected clean-copy verification baseline: `0bd93dea9d7035426b34600a6bae13277b3f08bd`; includes partial-write completeness/importability and protected-root junction/symlink rejection regressions.

> Checkbox ownership: only this change's Library apply-owner updates `tasks.md`. For each implementation task, run its verification, inspect allowed-file scope, commit the implementation, record the full SHA, and only then mark the checkbox complete in a follow-up OpenSpec state commit. Subagents, if used, may edit only explicitly assigned implementation/test files and never this change or `tasks.md`.
