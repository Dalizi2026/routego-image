## 1. Codex First-Run Routing

- [ ] 1.1 `[lane=integration; depends=planning artifacts]` Update the bilingual Skill so an unconfigured generation/edit/batch request opens a fresh Studio session, preserves the requested creative operation for continuation, never requests credentials in chat, and reports launch failure without URL reuse. Add packaged-Skill assertions in `packages/integration/test/package.test.ts`. Allowed implementation files: `skills/routego-image/SKILL.md`, `packages/integration/test/package.test.ts`. Verification: Integration package test, exact seven tool names, package build/verification, no key/path/token text leakage, safety, diff check.

## 2. Studio First-Run Experience

- [ ] 2.1 `[lane=studio; depends=1.1]` Add a pure readiness helper and initialize Studio on Settings when the active redacted configuration lacks a profile, key, or model; retain Workbench as the configured initial route. Add focused state tests. Allowed implementation files: `packages/studio/src/app/state.ts`, `packages/studio/src/app/StudioApp.tsx`, `packages/studio/test/shell-state.test.ts`. Verification: focused Studio state tests, typecheck, existing shell behavior, diff check.
- [ ] 2.2 `[lane=studio; depends=2.1]` Add the bilingual first-run status band to the existing Settings form, focus the real form from its primary action, show completion only after validated active/key/model state, and navigate to Workbench on explicit user action. Preserve write-only key clearing and all explicit model/probe/generation gates. Allowed implementation files: `packages/studio/src/features/settings/SettingsWorkspace.tsx`, `packages/studio/src/features/settings/settings.css`, `packages/studio/src/features/settings/types.ts`, `packages/studio/src/app/StudioApp.tsx`, `tests/browser/studio.spec.ts`, `tests/browser/harness.ts`. Verification: focused Studio tests, full Playwright unconfigured/configured/bilingual/mobile/security journeys, no automatic provider/model/probe/generation request, no credential rendering, typecheck/build, safety, diff check.

## 3. Offline Conformance

- [ ] 3.1 `[lane=integration; depends=2.2]` Run strict OpenSpec validation, repository safety, full typecheck/build/tests/exports/browser checks, deterministic plugin packaging and temporary-install smoke. Record the verified artifact hash without changing the installed plugin, marketplace, real HOME/CODEX_HOME configuration, provider state, or external CI. Allowed state files after implementation commit: this `tasks.md` only. Verification: all listed gates and clean Git scope.
