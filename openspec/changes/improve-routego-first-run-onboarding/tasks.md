## 1. Codex First-Run Routing

- [x] 1.1 `[lane=integration; depends=planning artifacts]` Update the bilingual Skill so an unconfigured generation/edit/batch request opens a fresh Studio session, preserves the requested creative operation for continuation, never requests credentials in chat, and reports launch failure without URL reuse. Add packaged-Skill assertions in `packages/integration/test/package.test.ts`. Allowed implementation files: `skills/routego-image/SKILL.md`, `packages/integration/test/package.test.ts`. Verification: 33/33 focused package tests passed; exact seven tool names and secret-safe current-call Studio rules preserved. Implementation commit: `b75c76ae219d02957a96c5ac4369e10050b03184`.

## 2. Studio First-Run Experience

- [x] 2.1 `[lane=studio; depends=1.1]` Add a pure readiness helper and initialize Studio on Settings when the active redacted configuration lacks a profile, key, or model; retain Workbench as the configured initial route. Add focused state tests. Allowed implementation files: `packages/studio/src/app/state.ts`, `packages/studio/src/app/StudioApp.tsx`, `packages/studio/test/shell-state.test.ts`. Verification: 5/5 focused state tests and Studio typecheck passed; configured Workbench routing is preserved. Implementation commit: `af4800765c1555727fa7f9168704655043820c91`.
- [x] 2.2 `[lane=studio; depends=2.1]` Add the bilingual first-run status band to the existing Settings form, focus the real form from its primary action, initialize a new profile for write-only key replacement and active selection, show completion only after validated active/key/model state, and navigate to Workbench on explicit user action. Existing-profile edits keep `unchanged` secret semantics. Add an explicit unconfigured option to the deterministic mock only for end-to-end browser verification. Preserve write-only key clearing and all explicit model/probe/generation gates. Allowed implementation files: `packages/studio/src/features/settings/SettingsWorkspace.tsx`, `packages/studio/src/features/settings/settings.css`, `packages/studio/src/features/settings/types.ts`, `packages/studio/src/features/settings/state.ts`, `packages/studio/test/settings-state.test.ts`, `packages/studio/src/app/StudioApp.tsx`, `packages/mock-relay/src/service/mock-service.ts`, `tests/browser/studio.spec.ts`, `tests/browser/harness.ts`. Verification: Studio 143/143 tests, Mock 44/44 tests, full Playwright 12/12, typecheck/build, first-run security and mobile checks passed. Implementation commit: `7a2a8d1b3db92313a14aee07a0f87c20c57c1c33`.
- [x] 2.3 `[lane=integration; depends=1.1]` Add the permanent configuration starter action and make plugin-page subtitle, long description, and all three supported starter actions Chinese/English two-line strings. Route explicit first-time or later configuration requests to a fresh Studio Settings session without chat credentials. Allowed implementation files: `.codex-plugin/plugin.json`, `skills/routego-image/SKILL.md`, `scripts/verify-plugin-package.mjs`, `packages/integration/test/package.test.ts`. Verification: plugin-creator and Skill validation passed; 33/33 canonical package tests passed with exactly three bilingual actions and exact seven tools. Implementation commit: `b91b32a5f25cc85e9e4f529d9e72e771c3cbc4d0`.
- [x] 2.4 `[lane=integration; depends=2.3]` Replace one-use bootstrap consumption with same-session authorization through the existing short launch TTL so Codex preview followed by user open succeeds. Preserve exact expiry, no-store HTML, query removal, constant-time token matching, bounded sessions, and rejection of launch tokens on API routes. Allowed implementation files: `packages/integration/src/runtime/sessions.ts`, `packages/integration/src/runtime/http-host.ts`, `packages/integration/test/http-host.test.ts`, `scripts/smoke-plugin-install.mjs`. Verification: Integration 269/269 tests passed, including manager and real-host preview-then-open/expiry/API-rejection coverage; explicit temporary installed-runtime preview replay, Integration typecheck/build, repository safety, seven-package exports, and diff check passed. Implementation commit: `b9eec4333a9c0f9462ad15b65b7373e8d4fe7123`.
- [x] 2.5 `[lane=studio; depends=2.4]` Consume the server-injected `__ROUTEGO_STUDIO_SESSION__` bootstrap object in the packaged Studio entrypoint before URL cleanup, retain URL-token handling only for deterministic development fixtures, and add browser coverage that opens the real bootstrap handoff without an address-bar API token. Allowed implementation files: `packages/studio/src/api/session.ts`, `packages/studio/test/session.test.ts`, `tests/browser/harness.ts`, `tests/browser/studio.spec.ts`, `scripts/smoke-plugin-install.mjs`. Verification: focused session and entry tests 6/6, production-style browser bootstrap regression 1/1, installed-package smoke 5/5, explicit packaged-runtime smoke, Studio typecheck/build, strict OpenSpec, and diff check passed. Implementation commit: `8f00c7750f3d8b2cd9c774815513301694ee0398`.
- [ ] 2.6 `[lane=integration; depends=2.5]` Admit an omitted browser `Origin` only through exact loopback Host and same-origin Fetch Metadata inference, preserving token, cookie, cross-site, foreign-origin, and direct-client rejection. Make the installed-package smoke use the browser-shaped status request. Allowed implementation files: `packages/integration/src/runtime/http-host.ts`, `packages/integration/test/http-host.test.ts`, `scripts/smoke-plugin-install.mjs`. Verification: real-host browser-shaped GET acceptance and rejection matrix, installed-package smoke, Integration typecheck/build, safety, and diff check. Implementation commit: pending.

## 3. Offline Conformance

- [ ] 3.1 `[lane=integration; depends=2.2,2.3,2.4,2.5,2.6]` Rebuild the plugin twice, update only the accepted offline artifact-manifest fingerprint in `scripts/smoke-plugin-install.mjs`, then run strict OpenSpec validation, repository safety, full typecheck/build/tests/exports/browser checks, deterministic plugin packaging and temporary-install smoke. Record the verified candidate artifact hash without changing the installed plugin, marketplace, real HOME/CODEX_HOME configuration, provider state, or external CI. Allowed implementation file: `scripts/smoke-plugin-install.mjs`. Allowed state file after implementation commit: this `tasks.md` only. Verification: both packages byte-identical, exact accepted hash, all listed gates, and clean Git scope.
