## 1. Codex First-Run Routing

- [x] 1.1 `[lane=integration; depends=planning artifacts]` Update the bilingual Skill so an unconfigured generation/edit/batch request opens a fresh Studio session, preserves the requested creative operation for continuation, never requests credentials in chat, and reports launch failure without URL reuse. Add packaged-Skill assertions in `packages/integration/test/package.test.ts`. Allowed implementation files: `skills/routego-image/SKILL.md`, `packages/integration/test/package.test.ts`. Verification: 33/33 focused package tests passed; exact seven tool names and secret-safe current-call Studio rules preserved. Implementation commit: `b75c76ae219d02957a96c5ac4369e10050b03184`.

## 2. Studio First-Run Experience

- [x] 2.1 `[lane=studio; depends=1.1]` Add a pure readiness helper and initialize Studio on Settings when the active redacted configuration lacks a profile, key, or model; retain Workbench as the configured initial route. Add focused state tests. Allowed implementation files: `packages/studio/src/app/state.ts`, `packages/studio/src/app/StudioApp.tsx`, `packages/studio/test/shell-state.test.ts`. Verification: 5/5 focused state tests and Studio typecheck passed; configured Workbench routing is preserved. Implementation commit: `af4800765c1555727fa7f9168704655043820c91`.
- [x] 2.2 `[lane=studio; depends=2.1]` Add the bilingual first-run status band to the existing Settings form, focus the real form from its primary action, initialize a new profile for write-only key replacement and active selection, show completion only after validated active/key/model state, and navigate to Workbench on explicit user action. Existing-profile edits keep `unchanged` secret semantics. Add an explicit unconfigured option to the deterministic mock only for end-to-end browser verification. Preserve write-only key clearing and all explicit model/probe/generation gates. Allowed implementation files: `packages/studio/src/features/settings/SettingsWorkspace.tsx`, `packages/studio/src/features/settings/settings.css`, `packages/studio/src/features/settings/types.ts`, `packages/studio/src/features/settings/state.ts`, `packages/studio/test/settings-state.test.ts`, `packages/studio/src/app/StudioApp.tsx`, `packages/mock-relay/src/service/mock-service.ts`, `tests/browser/studio.spec.ts`, `tests/browser/harness.ts`. Verification: Studio 143/143 tests, Mock 44/44 tests, full Playwright 12/12, typecheck/build, first-run security and mobile checks passed. Implementation commit: `7a2a8d1b3db92313a14aee07a0f87c20c57c1c33`.

## 3. Offline Conformance

- [ ] 3.1 `[lane=integration; depends=2.2]` Rebuild the plugin twice, update only the accepted offline artifact-manifest fingerprint in `scripts/smoke-plugin-install.mjs`, then run strict OpenSpec validation, repository safety, full typecheck/build/tests/exports/browser checks, deterministic plugin packaging and temporary-install smoke. Record the verified candidate artifact hash without changing the installed plugin, marketplace, real HOME/CODEX_HOME configuration, provider state, or external CI. Allowed implementation file: `scripts/smoke-plugin-install.mjs`. Allowed state file after implementation commit: this `tasks.md` only. Verification: both packages byte-identical, exact accepted hash, all listed gates, and clean Git scope.
