## 1. Provider adapters and codecs

- [x] 1.1 `[lane=creation; owner=main agent; deps=planning artifacts]` Add the Creation test harness plus Node-only runtime/provider types, resolved image-file reading and PNG/JPEG/WebP metadata validation, target/supporting/reference ordering, mask size/alpha/dimension validation, effective-control planning, and Tier A/B/C request serializers. Allowed files: `packages/creation/package.json`, `packages/creation/vitest.config.ts`, `packages/creation/tsconfig.json`, `packages/creation/src/provider/types.ts`, `packages/creation/src/provider/image-inputs.ts`, `packages/creation/src/provider/requests.ts`, `packages/creation/src/provider/index.ts`, and `packages/creation/test/provider-request.test.ts`. Forbidden: root/lock/shared schemas, provider response/execution/runtime modules, Library/Studio code, locator resolution, persistence, and real credentials/images. Verification before implementation commit: fresh `openspec instructions apply --change add-routego-image-creation --json`; `pnpm --filter @routego-image/creation typecheck`; focused provider-request tests covering Tier A text/image/images, Tier B JSON/multipart order/mask, Tier C state/input/control shapes, unknown/supported/unsupported/degraded gating, invalid files/masks, no endpoint derivation, and redaction; `pnpm safety`; `git diff --check`; scope review. Implementation commit: `e40feb43d5ce72623c82286ad40183573feb6ea3`.
- [x] 1.2 `[lane=creation; owner=main agent; deps=1.1]` Implement bounded synchronous Images/Responses parsing, Base64/magic/MIME validation, safe result-URL downloads and redirects, SSE framing/normalization, provider error mapping, artifact IDs/relationships, and partial-output preservation. Allowed files: `packages/creation/src/provider/sse.ts`, `packages/creation/src/provider/downloads.ts`, `packages/creation/src/provider/responses.ts`, `packages/creation/src/provider/errors.ts`, the minimum export update to `packages/creation/src/provider/index.ts`, and `packages/creation/test/provider-response.test.ts`. Forbidden: request serializer rewrites beyond an identified root-cause fix, execution/retry/batch/runtime code, shared contracts/mock changes, persistence, external network, or secret-bearing fixtures. Verification before implementation commit: fresh apply instructions; Creation typecheck; focused response tests for Base64/URL/JSON, detected formats, byte limits, redirect/authorization policy, LF/CRLF/multiline/fragmented/DONE SSE, Responses partial/final/failure, authentication/moderation/429/5xx mapping, partial billing flags, and recursive redaction; `pnpm safety`; `git diff --check`; scope review. Implementation commit: `85ea62d2654413381a0c1d9cb1db4ce74810c892`.

## 2. Image task execution

- [x] 2.1 `[lane=creation; owner=main agent; deps=1.1,1.2]` Implement the resolved-request executor and generate/edit service methods with provider context injection, frozen route selection, staged deadlines/cancellation, native versus same-transport fan-out variants, safe same-transport retry/backoff, native/degraded continuation, event callbacks, and schema-valid result assembly. Allowed files: `packages/creation/src/execution/types.ts`, `packages/creation/src/execution/retry.ts`, `packages/creation/src/execution/executor.ts`, `packages/creation/src/execution/service.ts`, `packages/creation/src/execution/index.ts`, and `packages/creation/test/executor.test.ts`. Forbidden: batch scheduler, transport adapters, Studio locator resolution, upload/Library storage, capability mutation/probing, chromakey processing, shared schemas, or root dependencies. Verification before implementation commit: fresh apply instructions; Creation typecheck; focused executor tests for success/unavailable, all four capability states, native/fan-out variants and provider counts, retry eligibility/limit/Retry-After, no timeout/auth/moderation/partial/cross-transport replay, stage deadlines/cancellation, Responses state, resolved degraded continuation, and false-success rejection; `pnpm safety`; `git diff --check`; scope review. Implementation commit: `086d2ea47f62fc337d4b1f9435dfe4f500253358`.
- [x] 2.2 `[lane=creation; owner=main agent; deps=2.1]` Implement bounded ordered batch scheduling and public batch service behavior with concurrency 1-10, exact task order/identity, per-item isolation, partial status derivation, cancellation of pending work, and preservation of completed results. Allowed files: `packages/creation/src/execution/batch.ts`, the minimum export/service update in `packages/creation/src/execution/index.ts` and `packages/creation/src/execution/service.ts`, and `packages/creation/test/batch.test.ts`. Forbidden: provider codec changes except a proven common-root fix, HTTP/MCP code, Library persistence, Studio locator projection, shared schemas, and root files. Verification before implementation commit: fresh apply instructions; Creation typecheck; focused batch tests for concurrency bounds, ordered mixed success/partial/failure, task isolation, cancellation, provider-request accounting, and schema-invalid input/output; full Creation tests to date; `pnpm safety`; `git diff --check`; scope review. Implementation commit: `b718f7f9549faa124e12132332b88a444fe4408a`.

## 3. Runtime transports

- [x] 3.1 `[lane=creation; owner=main agent; deps=2.2]` Implement the dependency-free STDIO JSON-RPC/MCP runtime with initialize lifecycle, newline framing, JSON Schema derived from frozen Zod inputs, exact seven-tool listing, schema-valid tool dispatch, structured errors, final image content projection, sanitized stderr diagnostics, and clean shutdown without forced process exit. Allowed files: `packages/creation/src/runtime/mcp/protocol.ts`, `packages/creation/src/runtime/mcp/server.ts`, `packages/creation/src/runtime/mcp/index.ts`, and `packages/creation/test/mcp.test.ts`. Forbidden: new MCP tools, MCP SDK/dependencies, HTTP code, Studio operation exposure, service business logic, shared contracts, manifest/release files, and real stdio process termination in tests. Verification before implementation commit: fresh apply instructions; Creation typecheck; focused MCP tests for initialize/list/call/errors/framing, exact seven names, schema generation, input/output failure closure, JSON/image content, UTF-8, redaction, and continued serving after success; `pnpm safety`; `git diff --check`; scope review. Implementation commits: `2b3c7a4a7d6f566d4d9869001a07483ccc1109b9` (mandatory compaction checkpoint), `1f8c26d43ea99dd9ee541a38e6fd3f61a2a6807a`.
- [x] 3.2 `[lane=creation; owner=main agent; deps=2.2]` Implement reusable loopback HTTP JSON/SSE runtime primitives with loopback-only binding, injected session/origin/CORS policy, bounded GET/POST decoding, frozen public/Studio registry dispatch, output validation, structured errors, Studio event broker/SSE serialization, disconnect cleanup, and opaque Integration extension hooks for upload/resource routes. Allowed files: `packages/creation/src/runtime/http/types.ts`, `packages/creation/src/runtime/http/dispatcher.ts`, `packages/creation/src/runtime/http/server.ts`, `packages/creation/src/runtime/http/sse.ts`, `packages/creation/src/runtime/http/index.ts`, and `packages/creation/test/http.test.ts`. Forbidden: Library/upload filesystem resolution or storage, binary upload/resource implementation, static Studio assets, provider/executor rewrites beyond a proven common-root fix, shared contracts, public route changes, and root dependencies. Verification before implementation commit: fresh apply instructions; Creation typecheck; focused HTTP tests for IPv4/IPv6 loopback, invalid bind, exact origin/token/CORS/cookie policy, GET/POST schemas, public and Studio dispatch, invalid input/output, body limits, extension delegation, started/partial/completed/failed SSE, billing flags, disconnect/cancellation, shutdown, UTF-8, and redaction; full Creation tests to date; `pnpm safety`; `git diff --check`; scope review. Implementation commit: `41f7c2b49a6d0483c82551d457cfb9349ec11a13`.

## 4. Package integration and regression

- [x] 4.1 `[lane=creation; owner=main agent; deps=1.1,1.2,2.1,2.2,3.1,3.2]` Review and export the approved provider/execution/runtime surface, add package-level cross-module regression tests and test-only Foundation mock-relay aliasing, and verify the Creation build/export contains no undeclared/native runtime dependency or cross-lane product implementation. Allowed files: `packages/creation/src/index.ts`, `packages/creation/vitest.config.ts`, the minimum package/tsconfig updates within `packages/creation/**`, and `packages/creation/test/integration.test.ts`. Forbidden: root/lock/workspace/shared schemas, mock-relay implementation, Library/Studio code, plugin manifest/install/release, or new third-party dependency. Verification before implementation commit: fresh apply instructions; `pnpm --filter @routego-image/creation typecheck`; `pnpm --filter @routego-image/creation test`; `pnpm --filter @routego-image/creation build`; offline mock-relay contract flows for Tier A text/image, Images multipart, Responses JSON/SSE partial, retry and batch; package export smoke; dependency/native audit; exact seven public tools; source search proving no Library/upload locator resolution or persisted-config read; `pnpm safety`; `git diff --check`; scope review. Implementation commit: `be67086dcb9caf9a1cd86c4e214b6f55bdcc6143`.

## 5. Final conformance and delivery

- [x] 5.1 `[lane=creation; owner=main agent; deps=4.1]` Run the complete Creation gate, re-read proposal/design/all four specs/tasks/PD-004/thread state for planning consistency, and record immutable implementation/task SHAs before notifying Program Controller generation 1. If any failure requires code changes, return to the owning task and repeat its focused verification/commit before continuing. Allowed files after the final implementation SHA: only this change's `tasks.md` task-state/delivery records. Forbidden: product changes during the final state-only step, shared/root/other-lane files, archive/release actions, or claiming real relay/cross-platform acceptance not performed here. Verification: fresh apply instructions; `openspec validate --all --strict --no-interactive`; `pnpm install --frozen-lockfile`; `pnpm safety`; `pnpm typecheck`; `pnpm build`; `pnpm test`; `pnpm check:exports`; Creation focused tests repeated; exact seven public operations; no endpoint guessing/cross-transport replay/locator resolution; runtime/native dependency audit; `git diff --check`; scope review against the planning commit; clean `git status`. Final message target: Program Controller generation 1 thread `019f715f-c042-7590-a38b-ffb406315903` with change, branch, full SHA, completed task count, validation commands, blockers, and residual risks. Planning commit: `c517b823cb4724367553f039769bd43643e78fe1`. Final implementation commit: `be67086dcb9caf9a1cd86c4e214b6f55bdcc6143`. Verified task-state head before this delivery record: `64afb9083ef6459d18d8aa17a77b3837e5ace030`.

> Checkbox ownership: only the Creation apply-owner updates this file. For tasks 1.1 through 4.1, run the listed verification, inspect scope, commit the implementation, record the full implementation SHA in the task line, and only then mark the checkbox complete in a follow-up OpenSpec state commit. No helper may edit this change or `tasks.md`.
