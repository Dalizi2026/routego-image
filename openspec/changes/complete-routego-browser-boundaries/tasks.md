## 1. Session upload lifecycle contracts

- [x] 1.1 `[lane=foundation-browser-boundary; owner=main agent; depends=planning artifacts]` Implement upload purpose/reservation/binary-route/finalize/status/discard schemas, lifecycle/reuse policy, integrity metadata, upload-specific structured errors, Studio upload service methods/operation definitions, exports, and focused success/failure/security tests. Allowed files: `packages/contracts/src/upload.ts`, minimum required `packages/contracts/src/errors.ts`, `packages/contracts/src/service.ts`, `packages/contracts/src/index.ts`, and focused `packages/contracts/test/**` upload/service tests. Forbidden: binary storage/streaming, filesystem reads, HTTP/session implementation, public MCP changes, dependencies/lockfile, or other packages. Verification before commit: `openspec instructions apply --change complete-routego-browser-boundaries --json`, contracts typecheck/test/build, all upload purposes and lifecycle failures, image reuse/ZIP single-consume schema assertions, bytes/Base64/path/credential rejection, public-operation freeze, browser-safe source audit, `pnpm safety`, and `git diff --check`. Implementation commit: `9fafdd96735d2f226b73766a17e668f7d5a5d921`.

## 2. Path-free Studio creation contracts

- [x] 2.1 `[lane=foundation-browser-boundary; owner=main agent; depends=1.1]` Implement strict `StudioImageInputRef`, generate/edit parameters, path-free artifacts/relationships/errors/results, started/partial/completed/failed SSE events, ordered batch schemas, `StudioCreationService`, internal operation definitions, protected resource compatibility, exports, and focused limits/invariant/security/freeze tests. Allowed files: `packages/contracts/src/studio-creation.ts`, minimum required `packages/contracts/src/errors.ts`, `packages/contracts/src/library.ts`, `packages/contracts/src/service.ts`, `packages/contracts/src/index.ts`, and focused `packages/contracts/test/**` creation/service tests. Forbidden: provider execution, resource resolution, SSE/HTTP transport, public path-based contract changes, public MCP additions, dependencies/lockfile, or downstream packages. Verification before commit: fresh apply instructions, contracts typecheck/test/build, text/reference/edit/mask-slot-zero/continuation/control matrix, success/partial/failure/degraded and SSE billing/output flags, ordered batch status, unsafe path/URL/Base64/credential rejection, public-operation freeze, browser-safe source audit, `pnpm safety`, and `git diff --check`. Implementation commit: `a65f9cd8d4d86c059c4b0a36335d127147c16fdc`.

## 3. Path-free search and settings mutation contracts

- [ ] 3.1 `[lane=foundation-browser-boundary; owner=main agent; depends=2.1]` Implement Studio Library search using the frozen public filter/cursor input and path-free asset/artifact/thumbnail results; implement complete defaults and output-directory `unchanged | default | clear | replace` mutation with literal local-path confirmation and redacted results; register local service methods/operations, exports, and focused search/settings/security/freeze tests. Allowed files: `packages/contracts/src/library.ts`, `packages/contracts/src/settings.ts`, minimum required `packages/contracts/src/service.ts`, `packages/contracts/src/index.ts`, and focused `packages/contracts/test/**` Library/settings/service tests. Forbidden: actual path validation/persistence, Library filesystem/ZIP logic, Studio UI, public search result changes, public MCP additions, dependencies/lockfile, or downstream packages. Verification before commit: fresh apply instructions, contracts typecheck/test/build, complete filter/sort/cursor semantics, detail/resource ID compatibility, no `path`/`filePath`, all settings mutation semantics and invalid confirmations, full-path result rejection, public-operation freeze, browser-safe source audit, `pnpm safety`, and `git diff --check`.

## 4. Deterministic stateful local-service mock

- [ ] 4.1 `[lane=foundation-browser-boundary; owner=main agent; depends=1.1,2.1,3.1]` Extend `MockRoutegoService` with isolated in-memory upload lifecycle state, non-empty aligned gallery seed, stable filtering/sorting/pagination, path-free Studio generate/edit/batch outcomes, settings mutation state reflected by reads, ZIP single consumption, and explicit success/partial/failure/degraded/expiry/type/size/checksum/discard/consumed fixtures. Allowed files: `packages/mock-relay/src/service/**`, `packages/mock-relay/src/index.ts` only if export wiring is required, and focused `packages/mock-relay/test/**` service tests. Forbidden: filesystem/environment/config/home/network reads, real credentials/images/bytes, relay/provider protocol changes unrelated to the local service, dependencies/lockfile, downstream packages, or OpenSpec edits by helpers. Verification before commit: fresh apply instructions, mock-relay typecheck/test/build, full upload and browser journey matrix, repeated fresh-instance determinism, stateful same-instance settings/upload assertions, gallery filter/cursor/ID alignment, path-free output checks, repository search proving no filesystem/environment/network read was added to the service mock, `pnpm safety`, and `git diff --check`.

## 5. Final conformance and delivery

- [ ] 5.1 `[lane=foundation-browser-boundary; owner=main agent; depends=1.1,2.1,3.1,4.1]` Run the complete Browser Boundary gate: strict OpenSpec, safety, typecheck, build, all tests, seven package exports, browser-safe source/declaration/emitted audits, public-operation freeze, deterministic mock repeat checks, native-runtime/dependency audit, diff-scope audit, and Git cleanliness. Re-read proposal, design, all delta/main specs, tasks, PD-002/PD-003/PD-004, thread state, and authoritative plan for planning consistency. If a failure requires code changes, return to the owning task and repeat its verification/commit sequence. After every check passes, record the final full SHA and send `[BROWSER_BOUNDARY_COMPLETE]` to the Program Controller with branch, SHA, task count, verification, blockers, and residual risks. Allowed files before the final implementation SHA: only this change's `tasks.md` task-state records after their owning commits; no product or dependency changes. Verification: fresh apply instructions, `openspec validate --all --strict --no-interactive`, `pnpm safety`, `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm check:exports`, browser-safe source/declaration/emitted inspection, exact seven-operation assertions, mock determinism, `git diff --check`, scope review against `bfc99ffd7d46dd3b8a5a45e981ff41136d8f3f93`, and clean `git status`.

> Checkbox ownership: only the Browser Boundary apply-owner updates this file. For tasks 1.1 through 4.1, run the listed verification, inspect scope, commit implementation, record the full implementation SHA in the task line, and only then mark the checkbox complete in a follow-up OpenSpec state commit. No subagent may edit this change or `tasks.md`.
