## 1. Workspace and provenance baseline

- [x] 1.1 `[lane=foundation owner=main deps=none files=root workspace/config only]` Create the pnpm/TypeScript strict ESM workspace, package manifests for `@routego-image/contracts`, `@routego-image/foundation`, and `@routego-image/mock-relay`, shared tsconfig/tsup/Vitest/Playwright configuration, Node `>=20.19` engines, root scripts, `.npmrc`, and ignore rules without native runtime dependencies. Verification: `pnpm install`, `pnpm typecheck`, and `pnpm build` succeed on the local platform.
- [x] 1.2 `[lane=foundation owner=main deps=1.1 files=pnpm-lock.yaml,root package metadata]` Resolve and commit the single root lockfile using workspace protocol dependencies and confirm production dependency closure contains no native addons or excluded upstream packages. Verification: `pnpm install --frozen-lockfile` and `pnpm list -r --prod` succeed.
- [x] 1.3 `[lane=foundation owner=main deps=audits files=THIRD_PARTY_NOTICES.md,licenses/**,docs/development/**]` Record upstream SHA `a10477581b3d43ac98d39777e4445625a9db113d`, full CookSleep MIT attribution/license, approved reuse boundaries, and an evidence-backed legacy compatibility checklist mapping `FZ-01` through `FZ-12`; do not modify or copy the legacy plugin. Verification: documented SHA/attribution match both audit documents and repository safety checks find no vendored upstream tree or user data.

## 2. Shared runtime contracts

- [x] 2.1 `[lane=foundation owner=main deps=1.1 files=packages/contracts/src/provider.ts,packages/contracts/src/common.ts]` Implement browser-safe Zod contracts for exact versus legacy endpoint input, independent optional endpoint URLs, provider protocols, scoped capability keys/states/evidence/limits, and redacted endpoint descriptors. Verification: contract tests cover default unknown states, all four states, transient-failure preservation, unsafe URL rejection, and explicit legacy normalization.
- [x] 2.2 `[lane=foundation owner=main deps=2.1 files=packages/contracts/src/image.ts,packages/contracts/src/errors.ts]` Implement strict image operation, reference/target/supporting/mask/invariant, requested/effective parameter, artifact/event, partial result, billing/output flag, and structured error schemas with limits for 16 references, 1–4 variants, 20 batch items, and concurrency 1–10. Verification: valid generate/edit/partial cases parse and ambiguous edit, mask-without-target, limit, compression, and unknown-field cases fail.
- [x] 2.3 `[lane=foundation owner=main deps=2.2 files=packages/contracts/src/service.ts,packages/contracts/src/tools.ts,packages/contracts/src/index.ts]` Implement the seven public tool input/result contracts, transport-neutral `RoutegoService` interface, operation identifiers, MCP tool names, loopback HTTP route metadata, and package exports without Node built-ins. Verification: browser-targeted build succeeds and a contract test proves MCP/HTTP mappings share the same schemas.

## 3. Provider policy and security primitives

- [ ] 3.1 `[lane=foundation owner=runtime-subagent deps=2.1,2.2 files=packages/foundation/src/provider/**,packages/foundation/src/index.ts]` Implement pure endpoint normalization and capability routing: exact endpoint by default, opt-in legacy base normalization, no derived models/Edits/Responses paths, scoped evidence transitions, billable-probe confirmation, degraded continuation, and no cross-transport replay decision. The `index.ts` change is limited to exporting the provider module and MUST NOT introduce any other public API change. Verification: focused Vitest cases cover Tier A text/image, Tier B/C gating, unknown/unsupported/degraded states, transient failures, and partial-output retry prohibition.
- [ ] 3.2 `[lane=foundation owner=runtime-subagent deps=2.2 files=packages/foundation/src/security/**,packages/foundation/src/index.ts]` Implement recursive non-mutating redaction, safe endpoint diagnostics, loopback bind/origin/session-token policy, constant-time token comparison, Windows/POSIX path containment with protected legacy roots, and default-no-authorization download policy including redirect decisions. The `index.ts` change is limited to exporting the security module and MUST NOT introduce any other public API change. Verification: focused Vitest cases cover nested/free-text secrets, URL credentials/query/fragment, CORS/CSRF rejection, traversal/cross-root cases, and credential stripping.

## 4. Service and relay test doubles

- [ ] 4.1 `[lane=foundation owner=runtime-subagent deps=2.3,3.2 files=packages/mock-relay/src/service/**,packages/mock-relay/src/index.ts]` Implement a deterministic schema-valid mock `RoutegoService` with success, failure, partial-success, degraded-continuation, and invalid-output fixtures; it must not read local config, keys, legacy paths, or user images. The `index.ts` change is limited to exporting the service module and MUST NOT introduce any other public API change. Verification: repeated fixture/request IDs produce stable validated results and configured failures never report false success.
- [ ] 4.2 `[lane=foundation owner=runtime-subagent deps=3.2,4.1 files=packages/mock-relay/src/relay/**,packages/mock-relay/src/index.ts]` Implement Web API mock relay handlers and an optional loopback test server for default text-only single endpoint, explicit single `image`/`images` data URL modes, Images generations/edits multipart shapes, and Responses JSON/SSE shapes; unconfigured sibling routes remain unavailable. The `index.ts` change is limited to exporting the relay module and MUST NOT introduce any other public API change. Verification: request observations contain only sanitized method/path/content-type/body shape and never credentials or image bytes.

## 5. Verification and repository safety

- [x] 5.1 `[lane=foundation owner=verification-subagent deps=2.1,2.2,2.3 files=packages/contracts/test/**]` Add exhaustive contract tests including Chinese prompts, emoji, spaces, Windows/POSIX paths/newlines, strict unknown-field rejection, tool/result/error schemas, variant versus batch semantics, and provider request/billing metadata. Verification: `pnpm --filter @routego-image/contracts test` passes.
- [ ] 5.2 `[lane=foundation owner=verification-subagent deps=3.1,3.2 files=packages/foundation/test/**]` Add provider/security regression tests for audit risks `R-01` through `R-15` that belong to Foundation, including no arbitrary URL bearer forwarding, no endpoint guessing, no unsupported inference from transient failures, UTF-8 preservation, and no forced-process-exit protocol. Verification: `pnpm --filter @routego-image/foundation test` passes.
- [ ] 5.3 `[lane=foundation owner=verification-subagent deps=4.1,4.2 files=packages/mock-relay/test/**]` Add offline fixtures/tests for single-endpoint text, Tier A `image` data URL, explicitly enabled `images`, standard Edits multipart ordering/mask slot, Responses JSON/SSE partial/final/failure, unavailable paths, and sanitized observations. Verification: `pnpm --filter @routego-image/mock-relay test` passes without credentials or external network.
- [ ] 5.4 `[lane=foundation owner=main deps=1.2,5.1,5.2,5.3 files=scripts/**,.github/workflows/**,root config]` Add the repository safety scanner, package export smoke checks, and Windows/Ubuntu/macOS Node 20.19+ CI for frozen install, safety, typecheck, build, and Foundation tests; provide Playwright config only, leaving browser test files to Studio. Verification: local root `pnpm safety`, `pnpm typecheck`, `pnpm build`, and `pnpm test` pass and workflow syntax is valid.

## 6. Contract freeze and delivery

- [ ] 6.1 `[lane=foundation owner=main deps=1.3,5.4 files=all Foundation-owned delivery files]` Run a clean-install regression from the committed lockfile, verify package exports contain no Node imports in the contracts browser surface and no native runtime dependency, and reconcile implementation against every requirement/scenario and audit recommendation. Verification: no planned item, hidden TODO, placeholder, fake data path, or uncommitted generated artifact remains.
- [ ] 6.2 `[lane=foundation owner=main deps=6.1 files=OpenSpec tasks/status only]` Run `openspec validate --all --strict --no-interactive`, `pnpm safety`, `pnpm typecheck`, `pnpm build`, `pnpm test`, and Git cleanliness checks; record final commits, tests, blockers, and residual risks for Program Controller integration. Verification: every command passes and the change reports all tasks complete before integration handoff.

> Checkbox ownership: only the Foundation apply-owner updates this file. A checkbox is marked complete only after its implementation is committed to the Foundation integration branch and its listed verification passes. Subagents must not edit OpenSpec artifacts, `tasks.md`, root dependency/lock files, or shared schemas.
