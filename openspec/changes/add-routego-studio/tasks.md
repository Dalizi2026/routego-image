## 1. Studio foundation and application shell

- [x] 1.1 `[lane=studio; owner=main agent; depends=planning artifacts; commit=fc7ef801ad38737ec948a1c9623fc14b3a7bc9fe]` Convert the importer scaffold into a buildable React/Vite application and Studio package library: update only the Studio package scripts/configuration, add `index.html`, strict TSX entry/export wiring, the contract-validating HTTP gateway, in-memory session-token bootstrap, protected resource/object-URL helpers, and an explicit-flag development Vite bridge backed by the read-only deterministic `MockRoutegoService`. Allowed files: `packages/studio/package.json`, `packages/studio/tsconfig*.json`, `packages/studio/tsup.config.ts`, `packages/studio/vite.config.ts`, `packages/studio/index.html`, `packages/studio/src/api/**`, `packages/studio/src/dev/**`, `packages/studio/src/main.tsx`, `packages/studio/src/index.ts`, and focused `packages/studio/test/**` gateway tests. Forbidden: shared contracts/mock edits, root files/lockfile, real HTTP/session enforcement, provider transport, persistence, and product feature pages. Verification before commit: fresh `openspec instructions apply --change add-routego-studio --json`, `pnpm --filter @routego-image/studio typecheck`, focused Studio tests, `pnpm --filter @routego-image/studio build`, mock bridge input/output validation, token/storage/log audit, `pnpm safety`, `git diff --check`, and scope review.
- [x] 1.2 `[lane=studio; owner=main agent; depends=1.1; scope-correction=fc6b07c7cb56aab07c37b03c2095d44a161b9596; commit=c132def5f96b9414437a2022f3cc349153945aa3]` Implement the production application shell and visual system: darkroom design tokens/CSS, responsive desktop/tablet/mobile navigation, Workbench/Library/Trash/Settings route state, Chinese/English dictionaries and toggle, accessible landmarks/focus/reduced-motion behavior, global loading/empty/error/degraded notifications, and reusable protected image/resource components. Allowed files: `packages/studio/src/app/**`, `packages/studio/src/components/**`, `packages/studio/src/i18n/**`, `packages/studio/src/styles/**`, focused `packages/studio/test/**` shell/helper tests, and a minimum integration update to `packages/studio/src/main.tsx` solely to render/inject the task-1.2 application shell after the existing secure session bootstrap and `HttpStudioGateway` construction. Forbidden: feature business flows, API/dev bridge changes, mock/shared/root edits, external fonts/assets, new dependencies, or weakening the task-1.1 launch-token-in-memory, visible-URL token removal, missing/rejected-session blocking, no-storage/no-log, and no-unprotected-retry behavior. Verification before commit: fresh apply instructions, existing session/gateway tests plus a focused entry-composition assertion or equivalent review proving secure bootstrap gates `StudioApp` rendering, Studio typecheck/tests/build, keyboard/ARIA and responsive helper assertions, no external/path/Base64 resource usage, `pnpm safety`, `git diff --check`, strict OpenSpec validation, exact scope review, and planning-consistency audit against `studio-application-shell`.

## 2. Creation workbench and capability states

- [x] 2.1 `[lane=studio; owner=main agent; depends=1.2; commit=d6df8b0213f1a814111333a2d8e2bf02ae5a0673]` Implement path-free upload and single-operation workbench flows: prompt/output controls, generate/edit modes, ordered reference/target/supporting inputs, reserve → binary PUT → finalize/discard state machine, edit invariants, client validation, `studioGenerate`/`studioEdit`, protected result rendering, effective parameters, retry/edit handoff, and preserved drafts on failure. Allowed files: `packages/studio/src/features/creation/**`, minimum integration updates to `packages/studio/src/app/**`, `packages/studio/src/components/**`, and focused `packages/studio/test/**` creation/upload tests. Forbidden: mask feature directory, Library/settings pages, provider execution, persistence, shared/root files, or fabricated local edits. Verification before commit: fresh apply instructions, Studio typecheck/tests/build, text-only and upload/reference/edit request assertions, no path/Base64/credential JSON, upload failure/expiry/discard handling, success/partial/failure result rendering helpers, `pnpm safety`, `git diff --check`, and spec audit.
- [x] 2.2 `[lane=studio; owner=main agent; depends=2.1; commit=a506802063ab3cbb2a96d4e9c14ed5fca0f61af2]` Implement scoped four-state capability evidence, required “当前中转未确认支持” disabled states, degraded explanations, probe-result integration, transparent/output-control gates, ordered 1–20 item batch editor with concurrency 1–10, per-item progress/outcomes, overall partial/failure presentation, and no automatic replay after output/billing risk. Allowed files: `packages/studio/src/features/creation/**`, `packages/studio/src/features/capabilities/**`, minimum integration updates to `packages/studio/src/app/**`, and focused `packages/studio/test/**` capability/batch tests. Forbidden: settings profile UI beyond shared capability state hooks, shared/provider routing edits, transport retries, or backend work. Verification before commit: fresh apply instructions, Studio typecheck/tests/build, unknown/supported/unsupported/degraded and transient-failure matrices, exact disabled message assertion, ordered mixed batch assertions, billing/output retry safeguards, `pnpm safety`, `git diff --check`, and planning-consistency audit against `studio-creation-workbench`.

## 3. Full-screen mask editor

- [x] 3.1 `[lane=studio; owner=mask-editor subagent; depends=1.2; may-run-parallel-with=2.1,2.2; commit=1919d3b1991a604a7e26c8eaf78a2a5c15a9ca8f]` Implement Studio-owned pure mask and viewport primitives plus the isolated full-screen editor: protected target display, fit/reset and cursor-centered zoom, explicit pan, pointer coordinate mapping/capture, brush, eraser, adjustable size, clear, bounded undo/redo, redo invalidation, overlay visibility/opacity, keyboard shortcuts, unsaved-close confirmation, empty-mask rejection, PNG blob creation, and a callback returning the finalized mask upload locator bound to target slot zero. Allowed files: `packages/studio/src/features/mask/**` and focused `packages/studio/test/mask-*.test.ts`. Forbidden: OpenSpec/tasks edits, workbench/app integration files, shared contracts/mock/root files, provider calls, or local-filter success claims. Verification before commit: fresh apply instructions (main agent supplies task context), Studio typecheck, mask-focused Vitest, canvas/coordinate/history/target-slot assertions, no source-image mutation or log output, build, `pnpm safety`, `git diff --check`, and scope review. Subagent returns commit SHA and test results; only main apply-owner updates this checkbox.
- [x] 3.2 `[lane=studio; owner=main agent; depends=2.2,3.1; commit=281d25cf0e232186ce3e4608ec6dc8c3b8d18098]` Review and integrate the mask editor into the edit workbench and upload lifecycle, enforce mask capability gating, target dimensions/setup state, finalized PNG upload, target-slot-zero request construction, failure recovery, and saved/unsaved editor transitions. Allowed files: minimum updates to `packages/studio/src/features/creation/**`, `packages/studio/src/features/mask/**`, `packages/studio/src/app/**`, and focused integration tests under `packages/studio/test/**`. Forbidden: shared/root/backend files or weakening subagent tests. Verification before commit: fresh apply instructions, review of the subagent commit/diff, Studio typecheck/all Studio tests/build, unsupported/supported mask flows, save/upload/failure/close regression tests, `pnpm safety`, `git diff --check`, and planning-consistency audit against `studio-mask-editor`.

## 4. Library, folders, trash, and comparison

- [x] 4.1 `[lane=studio; owner=main agent; depends=1.2; scope-correction=controller-89fe1524ae1cfd47377ced0f17ba6d77a180431d; commit=d3e229ac910b909554ecdeaa7fc6911a7e01d156]` Implement the path-free Library and Trash browsing experience: waterfall/card gallery, complete search/filter/sort/cursor pagination, loading/empty/error states, protected thumbnails, folder navigation, asset detail drawer, parameters/execution/errors/folders/allowed actions, related source/target/reference/supporting/mask/output resources, accessible source/result comparison, download, retry, and edit handoff. Allowed files: `packages/studio/src/features/library/**`, minimum integration updates to `packages/studio/src/app/**` and shared Studio components, `packages/studio/src/features/creation/CreationWorkbench.tsx` solely for a minimal validated identifier-only external handoff draft ingestion seam, plus focused `packages/studio/test/**` Library/compare tests. The handoff seam must consume a stable handoff once, replace the current draft through existing state/cleanup logic, preserve upload/mask cleanup, and must not use browser storage, URL payloads, module/global mutable channels, or custom window events. Existing Creation types/helpers remain read-only. Forbidden: Library persistence/index code, shared contracts/mock/root files, arbitrary paths, fake assets, or any other Creation file. Verification before commit: fresh apply instructions, Studio typecheck/tests/build, search/filter/pagination/path-free assertions, detail/resource/relationship alignment, comparison keyboard/bounds tests, retry/edit locator assertions, identifier-only one-shot ingestion/route-transition/draft-cleanup assertions, no path/storage/global-channel leakage, `pnpm safety`, `git diff --check`, and spec audit.
- [ ] 4.2 `[lane=studio; owner=main agent; depends=4.1,2.1]` Implement folders and safe mutation workflows: create/rename/reorder, multi-folder assignment/removal, multi-selection, soft delete, restore, 30-day trash messaging, permanent-delete preflight/confirmation, per-item partial outcomes, protected download/ZIP export, ZIP upload/import single-consume flow, refresh behavior, and conflict/failure recovery. Allowed files: `packages/studio/src/features/library/**`, minimum integration updates to `packages/studio/src/app/**` and shared Studio components, plus focused `packages/studio/test/**` mutation tests. Forbidden: filesystem/ZIP/deletion implementation, public path-based manage actions for Studio ZIP bytes, shared/root/backend edits, or bypassing preflight. Verification before commit: fresh apply instructions, Studio typecheck/tests/build, folder ordering and assignment tests, soft-delete/restore/permanent-delete exact confirmation, partial per-item outcomes, ZIP reserve/finalize/preflight/consume assertions, no path exposure, `pnpm safety`, `git diff --check`, and planning-consistency audit against `studio-library-experience`.

## 5. Settings and provider capability management

- [ ] 5.1 `[lane=studio; owner=main agent; depends=1.2,2.2]` Implement settings reads and mutations: provider profile create/update/remove/activate, endpoint fields, write-only API-key unchanged/replace/clear control with immediate clearing, non-billable model refresh, explicitly confirmed billable capability probes, four-state evidence/degradation/transient failure presentation, complete defaults update, output-directory unchanged/default/clear/confirmed replace, redacted results, and workbench-default synchronization. Allowed files: `packages/studio/src/features/settings/**`, minimum integration updates to `packages/studio/src/features/capabilities/**`, `packages/studio/src/app/**`, and focused `packages/studio/test/**` settings tests. Forbidden: real config persistence/path validation, logging or retaining secrets/full paths, shared/root/backend edits, or automatic billable probes. Verification before commit: fresh apply instructions, Studio typecheck/tests/build, profile/secret lifecycle tests, refresh versus confirmed probe assertions, four-state/transient matrix, defaults/output-directory semantics and redaction, DOM/storage/log secret scan, `pnpm safety`, `git diff --check`, and planning-consistency audit against `studio-settings-experience`.

## 6. Browser journeys and responsive verification

- [ ] 6.1 `[lane=studio; owner=browser-test subagent; depends=1.2,2.2,3.2,4.2,5.1]` Add the Studio-owned serial Playwright harness and browser journeys using the existing root configuration and explicit mock Vite flag: secure boot, localization, desktop/mobile navigation, capability-unavailable controls, confirmed probes, text generation, image upload/reference generation, edit/mask, partial/failure/degraded results, ordered batch, gallery/search/detail/comparison, folders/trash/mutations/ZIP, settings/secret redaction, keyboard focus, reduced motion, and responsive viewports. Allowed files: `tests/browser/**` only. Forbidden: OpenSpec/tasks edits, root Playwright config, package/app/shared/mock/backend files, real credentials/images/network, or weakening assertions. Verification before commit: fresh apply instructions (main agent supplies task context), `pnpm --filter @routego-image/studio build`, `pnpm test:browser`, repeat the deterministic core journey, inspect browser console/request capture for token/key/path/Base64/authorization leakage, `pnpm safety`, `git diff --check`, and scope review. Subagent returns commit SHA, command results, and residual risks; only main apply-owner updates this checkbox.

## 7. Final conformance and delivery

- [ ] 7.1 `[lane=studio; owner=main agent; depends=1.1,1.2,2.1,2.2,3.1,3.2,4.1,4.2,5.1,6.1]` Run the complete Studio gate in a clean verification state: fresh apply instructions, `openspec validate --all --strict --no-interactive`, `pnpm safety`, Studio typecheck/tests/build, root typecheck/build/tests/package exports, Playwright browser suite, browser-safe source/declaration/emitted inspection, exact seven-public-operation audit, responsive/accessibility/secret/path/resource review, diff scope against the frozen Browser Boundary baseline, `git diff --check`, and final Git cleanliness. Re-read proposal, design, all five Studio specs, tasks, six main specs, PD-004, plan, AGENTS, program and Studio thread state for planning consistency. If any failure requires changes, return to the owning task and repeat its verification/commit sequence. After all checks pass, update only allowed Studio delivery/task state, record the final full SHA, and send `[STUDIO_COMPLETE]` to Program Controller generation 1 thread `019f715f-c042-7590-a38b-ffb406315903` with branch, SHA, completed task count, delivered files, verification, blockers, and residual risks.

> Checkbox ownership: only the Studio apply-owner updates this file. For every implementation task, run the listed verification, inspect the exact file scope, commit implementation, record the full commit SHA in the task line, and only then mark the checkbox complete in a follow-up OpenSpec state commit. Subagents never edit OpenSpec artifacts or `tasks.md`.
