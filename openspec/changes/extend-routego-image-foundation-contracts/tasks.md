## 1. Browser-safe shared contracts

- [x] 1.1 [lane: foundation-extension; owner: main agent; depends: planning artifacts] Implement settings/provider-profile schemas, non-billable model refresh, confirmed capability probes, separate Studio operation definitions, and composable `LocalRoutegoService` while preserving all seven public operation names. Allowed files: `packages/contracts/src/settings.ts`, `packages/contracts/src/service.ts`, `packages/contracts/src/index.ts`, and focused `packages/contracts/test/**` settings/service tests. Forbidden: provider transport, public MCP additions, Library detail/mutation schemas, and downstream packages. Verification before commit: `pnpm --filter @routego-image/contracts typecheck`, `pnpm --filter @routego-image/contracts test`, `pnpm --filter @routego-image/contracts build`, public-operation freeze assertions, browser-safe source import audit, and `pnpm safety`. Implementation commit: `05ff760cc09725fa0f34c08c241b136a38263e82`.
- [x] 1.2 [lane: foundation-extension; owner: main agent; depends: 1.1] Implement folder listing/reordering, complete asset detail/relationship/allowed-action schemas, protected browser resources, and Library mutation preflight/execution with ordered per-item partial results. Allowed files: `packages/contracts/src/library.ts`, the minimum required updates to `packages/contracts/src/service.ts` and `packages/contracts/src/index.ts`, and focused `packages/contracts/test/**` Library contract tests. Forbidden: filesystem persistence, ZIP processing, deletion logic, Studio UI, or changes to public MCP operation names. Verification before commit: `pnpm --filter @routego-image/contracts typecheck`, `pnpm --filter @routego-image/contracts test`, `pnpm --filter @routego-image/contracts build`, unsafe path/URL rejection tests, structured failure/partial-result tests, browser-safe source import audit, and `pnpm safety`. Implementation commit: `a39abb485ea6d2d3344eedb5b72e78ac78a7b3c7`.

## 2. Deterministic local-service mocks

- [ ] 2.1 [lane: foundation-extension; owner: main agent; depends: 1.2] Extend `MockRoutegoService` to implement the composed local service with synthetic settings, provider profiles, folders, asset details, all relationship roles, protected resource descriptors, preflight, and success/failure/partial/degraded mutation/probe fixtures. Allowed files: `packages/mock-relay/src/service/**`, `packages/mock-relay/src/index.ts` only if export wiring is required, and focused `packages/mock-relay/test/**` service tests. Forbidden: environment/config/home-directory reads, user images, real credentials, network provider calls, relay protocol expansion unrelated to the local service, or OpenSpec edits by any helper. Verification before commit: `pnpm --filter @routego-image/mock-relay typecheck`, `pnpm --filter @routego-image/mock-relay test`, `pnpm --filter @routego-image/mock-relay build`, repeated-fixture determinism, failure/partial/degraded assertions, repository search confirming no filesystem/config/network read was added to the service mock, and `pnpm safety`. Commit the implementation and record its full SHA before checking this task.

## 3. Downstream workspace importers

- [ ] 3.1 [lane: foundation-extension; owner: main agent; depends: 1.2] Add importer-only scaffolds for `packages/creation`, `packages/library`, and `packages/studio`; update root TypeScript/export verification and regenerate `pnpm-lock.yaml`. Creation and Library may declare workspace dependencies only. Studio may add only exact locked React, ReactDOM, Vite, `@vitejs/plugin-react`, `@types/react`, and `@types/react-dom` dependencies plus workspace packages. Allowed files: the three new package directories, root `package.json` only if required, `tsconfig.json`, `tsconfig.tooling.json`, `scripts/check-package-exports.mjs`, `pnpm-workspace.yaml` only if required, and `pnpm-lock.yaml`. Forbidden: product pages/components, provider transport, persistence, plugin manifest/install/release files, native runtime dependencies, or any other third-party package. Verification before commit: lockfile importer review, `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm check:exports`, runtime dependency/native-addon audit, importer source scope review, and `pnpm safety`. Commit the implementation and record its full SHA before checking this task.

## 4. Final conformance and delivery

- [ ] 4.1 [lane: foundation-extension; owner: main agent; depends: 1.1, 1.2, 2.1, 3.1] Run the complete Foundation Extension gate in a clean verification copy: frozen install, `openspec validate --all --strict --no-interactive`, safety, typecheck, build, all tests, package exports, browser-safe contracts with no Node built-in imports, runtime/native dependency audit, package importer checks, Git diff scope, and final Git cleanliness. Re-read proposal, design, all delta/main specs, tasks, PD-002, and the thread state for a planning-consistency audit. If any failure requires code changes, return to the owning task and re-run its verification before committing. After all checks pass, update only the Foundation Extension thread delivery state, commit the final task-state/delivery record, mark this task complete, record the final full SHA, and send `[FOUNDATION_EXTENSION_COMPLETE]` to the Program Controller with branch, SHA, task count, verification, blockers, and residual risk.
