## 1. PD-005 durable source-rendition gate

- [x] 1.1 `[lane=integration; owner=main agent; depends=planning artifacts]` Add the Library-only `source | partial | final` rendition phase, 33-rendition detail bound, output-only primary invariants, and exact relationship artifact ownership while freezing public `ImageArtifact.phase=partial|final` and the seven public tools. Allowed implementation files: `packages/contracts/src/library.ts`, `packages/contracts/test/library-service.test.ts`. Forbidden: `packages/contracts/src/image.ts`, public operation/tool definitions, Creation, Library implementation, Studio, root dependencies, manifest/release files. Verification before implementation commit: fresh `openspec instructions apply --change integrate-routego-image-plugin --json`; contracts typecheck/tests/build; focused source/public-phase/bound/ownership cases; exact seven tool names; browser-safe contract audit; `pnpm safety`; `git diff --check`; exact scope review. Implementation commit: `0abce8690a35014b80aa8a5f93188ce08fca0608`.
- [x] 1.2 `[lane=integration; owner=main agent; depends=1.1]` Implement source/output co-ingestion on one preallocated operation asset, mixed source MIME, output-only primary/final invariants, the 33-rendition limit, exact relationship ownership, parsing/corruption rejection, and detail/resource projection. Allowed implementation files: `packages/library/src/gallery/model.ts`, `packages/library/src/gallery/assets.ts`, `packages/library/src/gallery/read-service.ts`, `packages/library/test/gallery/source-renditions.test.ts`. Forbidden: unrelated queries/folders/mutations/uploads/config, ZIP files, Creation/Studio/root files, public schema changes beyond task 1.1. Verification before implementation commit: fresh apply instructions; Library typecheck/build; focused mixed PNG/JPEG/WebP source/output, primary, succeeded/partial, 33/34 bound, dedupe, exact owner, corrupt index, detail/resource, traversal/legacy tests; full Library tests to date; safety; diff check; scope review. Implementation commit: `d7103c550fad4b7df4326849cc4471563bc5ff27`.
- [x] 1.3 `[lane=integration; owner=main agent; depends=1.2]` Preserve source rendition phases, exact artifact/relationship ownership, primary output, checksums, Unicode, collision remapping, and the 33-item bound through versioned ZIP export/import. Allowed implementation files: `packages/library/src/zip/manifest.ts`, `packages/library/src/zip/portability.ts`, `packages/library/test/zip/portability.test.ts`. Forbidden: ZIP codec/CRC changes, gallery implementation outside read-only APIs, config/upload changes, other lanes, root dependencies. Verification before implementation commit: fresh apply instructions; Library typecheck/build; focused source graph round-trip, ID collision/remap, CRC/SHA/MIME/size, source-primary rejection, succeeded-without-final rejection, 33/34 limits, Unicode, traversal, atomic failure, ZIP single-consume tests; full Library tests; safety; diff check; scope review. Implementation commit: `0b5ac07582ec6cdde9ceeb173a02ea72ac751428`.
- [x] 1.4 `[lane=integration; owner=main agent; depends=1.1,1.2]` Reconstruct Studio retry target/reference/supporting/mask inputs from ordered exact relationship artifact IDs and fail closed on missing, duplicated, ambiguous, or inconsistent ownership; keep edit-again deliberately targeted at the selected output. Allowed implementation files: `packages/studio/src/features/library/handoff.ts`, `packages/studio/test/library-handoff.test.ts`. Forbidden: Library workspace UI, Creation workbench, gateway, mock bridge, browser tests, backend/shared/root files. Verification before implementation commit: fresh apply instructions; Studio typecheck/tests/build; focused artifact-locator order/role/label, slot-zero mask, existing-owner, upload-source, primary-fallback rejection, ambiguity/failure, identifier-only/no-path tests; safety; diff check; scope review. Implementation commit: `34956e46182d7df8d7b5a6af98659ab2bb68f658`.

## 2. PD-005 authenticated Studio streaming gate

- [ ] 2.1 `[lane=integration; owner=main agent; depends=1.1,1.2,1.3,1.4]` Create the `@routego-image/integration` package scaffold and the injected authenticated `POST /api/v1/studio/creation/stream` generate/edit route that validates the frozen discriminated input and emits only schema-valid `StudioImageOperationEvent` values; add the approved pure-JS PNG dependency baseline without composing the production service yet. Allowed implementation files: `packages/integration/package.json`, `packages/integration/tsconfig.json`, `packages/integration/tsconfig.build.json`, `packages/integration/tsup.config.ts`, `packages/integration/vitest.config.ts`, `packages/integration/src/index.ts`, `packages/integration/src/runtime/stream-route.ts`, `packages/integration/test/stream-route.test.ts`, root `package.json`, `pnpm-lock.yaml`, `tsconfig.json`, `scripts/check-package-exports.mjs`. Exact new dependencies: runtime `pngjs@7.0.0`; development `@types/pngjs@6.0.5`; existing workspace dependencies only otherwise. Forbidden: another stream route, production composition/provider/image logic, HTTP host/static/upload/resource routes, plugin manifest/Skill, Creation edits, unrelated root configuration. Verification before implementation commit: fresh apply instructions; frozen install; Integration/package/root typecheck/build; focused exact route/session/origin/input/content-type, first/unique started, zero-or-more partial, one terminal, request-ID consistency, monotonic sequence, terminal-before-EOF, disconnect/channel cleanup and no `[DONE]`/sentinel tests with injected service; dependency/license/native audit; seven workspace package exports including Integration; exact seven public tools; safety; diff check; scope review.
- [ ] 2.2 `[lane=integration; owner=main agent; depends=2.1]` Add the browser-safe fetch-stream SSE parser and `StudioGateway` streaming method for exactly `POST /api/v1/studio/creation/stream`, using the in-memory session header, strict content type/UTF-8/framing/schema/state/request-ID/sequence/terminal/body limits, abort propagation, reader cancellation, and channel cleanup. Allowed implementation files: `packages/studio/src/api/sse.ts`, `packages/studio/src/api/gateway.ts`, `packages/studio/src/api/index.ts`, `packages/studio/test/sse.test.ts`, `packages/studio/test/gateway.test.ts`. Forbidden: workbench state/components, mock bridge, browser tests, backend/shared/root files, EventSource/query-token authentication, alternate streaming paths or `[DONE]` handling. Verification before implementation commit: fresh apply instructions; Studio typecheck/tests/build; LF/CRLF/multiline/fragmented records, strict MIME/UTF-8, bounded line/event/body, exactly one first started, zero/many partial, consistent request ID, strictly increasing sequence, exactly one completed|failed terminal, EOF only after terminal, missing/duplicate/late started, request-ID drift, missing/duplicate terminal, post-terminal data, `[DONE]`/non-schema sentinel rejection, abort/release, session header, redaction/no-path/Base64 tests; safety; diff check; scope review.
- [ ] 2.3 `[lane=integration; owner=main agent; depends=2.1,2.2]` Make the deterministic Studio mock handler and Vite bridge serve `POST /api/v1/studio/creation/stream` with genuinely chunked valid and invalid fixtures through the production parser rather than buffering a fake final response. Allowed implementation files: `packages/studio/src/dev/mock-handler.ts`, `packages/studio/src/dev/vite-mock-bridge.ts`, `packages/studio/test/mock-bridge.test.ts`. Forbidden: Foundation mock service/contracts, alternate stream routes, workbench UI/state, gateway parser except a proven common-root fix, root files, real network/images/credentials. Verification before implementation commit: fresh apply instructions; Studio typecheck/tests/build; focused real chunking, valid started/partial/completed and started/partial/failed, missing/duplicate/late started, request-ID drift, invalid sequence/schema, `[DONE]`/sentinel, missing/duplicate terminal, post-terminal, EOF-before-terminal, oversize, disconnect/abort channel cleanup, full-five-minute-capable and near-expiry descriptor fixtures, deterministic repeat, no token/key/path/Base64/log leakage tests; safety; diff check; scope review.
- [ ] 2.4 `[lane=integration; owner=main agent; depends=2.2,2.3]` Add minimal workbench streaming state and rendering: show arriving protected partials, promote completed results, preserve partial artifacts plus billing/output risk after failure, fail closed on invalid terminal data, and separate fetch/reader/channel plus browser object-URL cleanup from the immutable server descriptor expiry `min(registration + 5 minutes, owning session expiry)` on failure, disconnect, cancel, or unmount. Allowed implementation files: `packages/studio/src/features/creation/stream.ts`, `packages/studio/src/features/creation/types.ts`, `packages/studio/src/features/creation/result.ts`, `packages/studio/src/features/creation/CreationWorkbench.tsx`, `packages/studio/test/creation-stream.test.ts`. Forbidden: capability/product-rule changes, batch redesign, mask/Library/settings files, backend/shared/root files. Verification before implementation commit: fresh apply instructions; Studio typecheck/tests/build; exact state-machine transitions, failure-after-partial flags, reader/channel and browser object-URL cleanup without premature server revocation, normal and near-expiry descriptor projection, disconnect, cancel/unmount, descriptor/session expiry, shutdown/unavailable projection, invalid stream closure, draft preservation, no automatic replay/no path or secret tests; safety; diff check; scope review.
- [ ] 2.5 `[lane=integration; owner=main agent; depends=2.3,2.4]` Extend the existing serial browser journey with the exact authenticated stream route, valid streamed success, failure-after-partial with fetchable resource until the immutable session-capped descriptor expiry, invalid state/request/output closure, disconnect, abort, expiry, and cleanup while retaining existing responsive/security assertions. Allowed implementation file: `tests/browser/studio.spec.ts`. Forbidden: browser harness/root Playwright config, application/backend/shared files, real network/credentials/images, weakened assertions. Verification before implementation commit: fresh apply instructions; Studio build; complete `pnpm test:browser`; repeat deterministic streamed core journey; cover a session allowing the full five minutes and a near-expiry session, prove partial fetch succeeds immediately before exact descriptor expiry, fails at/after descriptor or session expiry, browser object-URL revocation does not revoke the server descriptor, and shutdown revokes immediately; console/request/resource audit for token/key/path/Base64/authorization leakage; safety; diff check; scope review.

## 3. Production provider, image, and composition layer

- [ ] 3.1 `[lane=integration; owner=main agent; depends=2.1,2.5]` Implement Library-owned provider context loading, redacted status health, explicitly configured non-billable model refresh, exact confirmed capability probes with deterministic synthetic PNG inputs, ephemeral probe evidence, four-state persistence, and bounded provider diagnostics. Allowed implementation files: `packages/integration/src/provider/context.ts`, `packages/integration/src/provider/models.ts`, `packages/integration/src/provider/probes.ts`, `packages/integration/src/image/png.ts`, `packages/integration/test/provider.test.ts`. Forbidden: Creation provider/executor edits, endpoint derivation, automatic/billable probes, real credentials/network in tests, Library config implementation changes. Verification before implementation commit: fresh apply instructions; Integration typecheck/tests/build; active/missing profile/key, exact endpoints, no `/models` derivation, bounded model shapes, confirmed/unconfirmed probe, supported/unsupported/degraded/transient matrices, one-request/billing flags, synthetic PNG, recursive redaction tests; root safety; diff check; scope review.
- [ ] 3.2 `[lane=integration; owner=main agent; depends=3.1]` Implement bounded output data-URL materialization, metadata/hash validation, exclusive request staging, session-owned ephemeral browser resources expiring at `min(registration + 5 minutes, owning session expiry)`, and PNG chromakey that retains provider-original bytes only transaction-locally, atomically uses processed bytes under the existing output identity on success, and falls back to the validated original under that identity on processing failure without transparent success, a second rendition, or a new relationship role. Allowed implementation files: `packages/integration/src/image/materialize.ts`, `packages/integration/src/image/chromakey.ts`, `packages/integration/src/runtime/ephemeral-resources.ts`, `packages/integration/test/image.test.ts`. Forbidden: Library persistence, service composition, HTTP host, Creation response parser edits, native dependencies, real images, any non-frozen relationship role. Verification before implementation commit: fresh apply instructions; Integration typecheck/tests/build; PNG/JPEG/WebP decode/claims/limits, multi-output partial preservation, transaction cleanup, exclusive collision, normal full-five-minute and near-expiry session descriptors, immutable expiry/ETag, fetch before and rejection at/after descriptor/session expiry, immediate shutdown revocation, chromakey alpha/same artifact identity/single persisted bytes, exact 17+12+4=33 worst case, complex-content refusal, processing-failure original fallback/no transparent success, synthetic-only and no-native audit; safety; diff check; scope review.
- [ ] 3.3 `[lane=integration; owner=main agent; depends=1.2,3.1]` Resolve ordered Studio asset/artifact/upload inputs through `RoutegoLibraryService`, validate upload purposes and mask slot zero, allocate operation/source artifact IDs, and construct the exact internal path-based Creation request plus durable relationship plan. Allowed implementation files: `packages/integration/src/composition/inputs.ts`, `packages/integration/src/composition/graph.ts`, `packages/integration/test/inputs.test.ts`. Forbidden: Library/Creation implementation edits, output persistence, HTTP/MCP/runtime files, schema changes. Verification before implementation commit: fresh apply instructions; Integration typecheck/tests/build; text-only, mixed target/reference/supporting/mask, order/label, existing vs upload ownership, expired/consumed/missing/corrupt locator, continuation previous output, 16+mask and slot-zero bounds, zero-provider-call failure tests; safety; diff check; scope review.
- [ ] 3.4 `[lane=integration; owner=main agent; depends=1.2,1.3,3.2,3.3]` Persist saved source/partial/final graphs atomically, project durable and session-capped ephemeral Studio resources, produce honest public paths/display data, enforce unsaved public output-directory requirements, and use Library's exclusive project-copy behavior while preserving exactly one chromakey output artifact identity per slot. Allowed implementation files: `packages/integration/src/composition/results.ts`, `packages/integration/test/results.test.ts`. Forbidden: service method routing, provider probing, HTTP/MCP, Library implementation changes, direct legacy paths, extra chromakey rendition/relationship roles. Verification before implementation commit: fresh apply instructions; Integration typecheck/tests/build; saved/unsaved Studio, public saved/unsaved, source/output graph, 33-bound chromakey graph, shared Codex/Studio IDs, partial ingestion, failure-terminal/disconnect/cancel resource lifetime without expiry shortening, normal/near-expiry descriptors, commit/copy failure, collision/no-overwrite, cleanup, output directory, no hidden Library record/path/secret tests; focused Library regressions; safety; diff check; scope review.
- [ ] 3.5 `[lane=integration; owner=main agent; depends=3.1,3.2,3.3,3.4]` Compose the complete production `LocalRoutegoService`: status/settings/refresh/probe, public generate/edit/batch/search/manage/openStudio delegation, Studio generate/edit/batch, Library/upload methods, execution events, cancellation, recovery, and schema validation through one object. Allowed implementation files: `packages/integration/src/composition/service.ts`, `packages/integration/src/index.ts`, `packages/integration/test/service.test.ts`. Forbidden: runtime HTTP/MCP process, manifest/package scripts, other-lane files, real provider/data tests. Verification before implementation commit: fresh apply instructions; Integration typecheck/all tests/build/export; exact LocalRoutegoService method matrix; same Library state across public/Studio; success/partial/failure/degraded/cancelled batch/order/provider counts; invalid input/output closure; recovery/health; exact seven tools; no cross-lane path/credential leakage; root safety/typecheck/build/tests/exports; diff check; scope review.

## 4. Loopback runtime and process lifecycle

- [ ] 4.1 `[lane=integration; owner=main agent; depends=3.5]` Implement the Integration-owned loopback HTTP host, bounded multi-session manager, token-protected no-store bootstrap HTML, allowlisted contained Studio static assets, exact MIME/ETag, listener reuse, and shutdown lifecycle while delegating JSON validation to Creation's dispatcher. Allowed implementation files: `packages/integration/src/runtime/sessions.ts`, `packages/integration/src/runtime/static.ts`, `packages/integration/src/runtime/http-host.ts`, `packages/integration/src/runtime/lifecycle.ts`, `packages/integration/test/http-host.test.ts`. Forbidden: upload/resource/stream route implementation, Studio source, Creation server edits, manifest/packaging, LAN/public bind. Verification before implementation commit: fresh apply instructions; Integration typecheck/tests/build; IPv4/IPv6, invalid bind, bootstrap token/expiry, multiple session reuse, URL token removal contract, static MIME/ETag/no listing/traversal/symlink, origin/cookie/CORS, listener start/reuse/stop, signal cleanup/redaction tests; safety; diff check; scope review.
- [ ] 4.2 `[lane=integration; owner=main agent; depends=2.1,3.2,3.5,4.1]` Implement protected upload staging, durable/ephemeral image and ZIP resource streaming, and real generate/edit wiring only at `POST /api/v1/studio/creation/stream`, with content-length/MIME/expiry/ETag enforcement, disconnect cancellation, reader/channel cleanup, and independent immutable partial-resource expiry at `min(registration + 5 minutes, owning session expiry)` through the HTTP host. Allowed implementation files: `packages/integration/src/runtime/routes.ts`, `packages/integration/src/runtime/stream-route.ts`, `packages/integration/test/routes.test.ts`, `packages/integration/test/stream-route.test.ts`. Forbidden: alternate stream routes, Library upload/resource implementation, Studio parser/workbench, Creation runtime edits, static/session redesign beyond a proven common-root fix. Verification before implementation commit: fresh apply instructions; Integration typecheck/tests/build; reserve/PUT/finalize/status/discard, oversize/MIME/checksum/expiry/session/origin, durable/ephemeral/ZIP resources, exact SSE state/request/sequence/terminal/EOF/sentinel rules, failed-terminal and disconnect/cancel reader/channel closure without expiry shortening, normal session/full-five-minute descriptor, near-expiry session/short descriptor, fetch immediately before exact expiry, rejection at/after descriptor or session expiry, immediate shutdown revocation, no bytes/Base64/path/key logs; Studio gateway integration tests; safety; diff check; scope review.
- [ ] 4.3 `[lane=integration; owner=main agent; depends=4.2]` Implement the plugin process/CLI entrypoint around Creation's `RoutegoMcpServer`, the production local service and HTTP lifecycle, UTF-8 framing, protocol-only stdout, redacted stderr, signal handling, and normal resource release without forced business `process.exit()`. Allowed implementation files: `packages/integration/src/runtime/mcp-process.ts`, `packages/integration/src/cli.ts`, `packages/integration/src/index.ts`, `packages/integration/test/mcp-process.test.ts`. Forbidden: new MCP tools, MCP SDK/dependencies, service business rewrites, manifest/Skill/packaging, real process termination in tests. Verification before implementation commit: fresh apply instructions; Integration typecheck/tests/build; initialize/list/call/errors/framing, exact seven names/schemas/content, HTTP coexistence, continued serving, SIGINT/SIGTERM bounded shutdown, stdout/stderr/redaction, Windows multi-fetch regression, no forced result exit; root safety/tests/exports; diff check; scope review.

## 5. Skill, manifest, packaging, and temporary installation

- [ ] 5.1 `[lane=integration; owner=main agent; depends=4.3]` Read and use `plugin-creator`, then create/update the canonical Routego Image 1.0.0 manifest, plugin-relative runtime launcher, and thin bilingual Skill for the exact seven MCP tools and current-call result discipline. Allowed implementation files: `.codex-plugin/plugin.json`, `skills/routego-image/SKILL.md`, `scripts/start-routego-image.mjs`. Forbidden: marketplace/cachebuster/reinstall execution, package artifact/build outputs, legacy plugin edits, public tool changes, credentials or hardcoded home/plugin paths. Verification before implementation commit: fresh apply instructions; plugin-creator manifest validation; JSON/UTF-8; launcher relative-root audit; Skill status/capability/variant/batch/edit/library/studio/current-call/redaction scenarios; exact seven names; no legacy CLI/path/key requests; safety; diff check; scope review.
- [ ] 5.2 `[lane=integration; owner=main agent; depends=5.1]` Build and verify the self-contained plugin staging artifact with bundled Integration runtime, Studio hashed assets, Skill/manifest/launcher, third-party notices/licenses, content hashes, dependency/license/native audit, reproducibility checks, and allowlist/denylist exclusion. Allowed implementation files: `packages/integration/tsup.config.ts`, `packages/integration/package.json`, root `package.json`, `pnpm-lock.yaml`, `.gitignore`, `scripts/build-plugin-package.mjs`, `scripts/verify-plugin-package.mjs`, `THIRD_PARTY_NOTICES.md`, `licenses/pngjs-MIT.txt`, `packages/integration/test/package.test.ts`. Generated staging/artifact files remain ignored and uncommitted. Forbidden: target installation, marketplace, release, tracked dist/build/cache/report, other-lane product edits. Verification before implementation commit: fresh apply instructions; frozen install; full build; two clean staged builds/content manifests; no node_modules/native/install scripts/unresolved external/source checkout/local paths/maps/secrets/config/user images/Library/caches; Studio assets and notices present; Node 20 bundle import; safety; diff check; scope review.
- [ ] 5.3 `[lane=integration; owner=main agent; depends=5.2]` Implement isolated temporary-`CODEX_HOME` install and fresh Codex process/task smoke for plugin discovery/version, Skill, exact-seven MCP, offline status/tools, Studio bootstrap/static/API/upload/resource/stream, shared synthetic Library, and legacy isolation. Allowed implementation files: `scripts/smoke-plugin-install.mjs`, `packages/integration/test/install-smoke.test.ts`. Temporary home/install/output files remain outside the repository and are removed safely after evidence capture. Forbidden: real installed plugin replacement, real HOME/CODEX_HOME data, real relay, marketplace, deployment/release. Verification before implementation commit: fresh apply instructions; packaged artifact verification; temporary environment smoke with fresh process; exact version/tools; self-contained/no source/node_modules access; Codex/Studio same Library; session/resource/stream; legacy hash/timestamp independence; cleanup; safety; diff check; scope review.

## 6. Offline end-to-end and cross-platform gate

- [ ] 6.1 `[lane=integration; owner=main agent; depends=1.1,1.2,1.3,1.4,2.5,3.5,4.3,5.3]` Add offline production-composition end-to-end tests for text, upload reference, direct edit, mask, degraded continuation, ordered partial batch, single-identity bounded chromakey, Library detail/retry/ZIP, project copy, MCP, exact HTTP stream route/state machine/session-capped resource lifetime, Studio, restart/recovery, and error/security paths. Allowed implementation files: `packages/integration/test/end-to-end.test.ts`, `packages/integration/test/security.test.ts`, `packages/integration/test/fixtures.ts`. Forbidden: product implementation changes except returning to the owning failed task, real network/credentials/user data, other-lane test weakening. Verification before implementation commit: fresh apply instructions; repeated deterministic focused suite; 17+12+4=33/no extra role, valid and adversarial SSE state/request/EOF/sentinel cases, normal session/full-five-minute descriptor, near-expiry session/short descriptor, failure/disconnect/cancel fetch immediately before exact expiry, rejection at/after descriptor or session expiry, browser object-URL cleanup independence, immediate shutdown revocation, Integration/root typecheck/build/tests/exports; Studio browser suite; exact seven/public phase freeze; native/dependency/secret/path audits; safety; diff check; scope review.
- [ ] 6.2 `[lane=integration; owner=main agent; depends=6.1]` Add Windows, Ubuntu, and macOS Node.js 20.19+ CI for frozen install, strict OpenSpec, safety, typecheck, build, all tests, exports, browser verification, package reproducibility, temporary-install structural/runtime smoke, and artifact/security audits. Allowed implementation files: `.github/workflows/ci.yml`, `scripts/ci-plugin-smoke.mjs`. Forbidden: secrets, real relay, marketplace/release, deployment, changed test thresholds, unrelated workflows. Verification before implementation commit: fresh apply instructions; workflow syntax; local command parity; explicit OS/Node matrix and caches; no credential permissions; package/smoke artifacts are temporary; root gates pass; safety; diff check; scope review.
- [ ] 6.3 `[lane=integration; owner=main agent; depends=6.2]` Run the complete offline Integration conformance gate in a clean verification copy and record immutable implementation/task SHAs before requesting any real-relay approval. Allowed files after the verified implementation SHA: this change's `tasks.md` task-state/commit records and `.codex/routego-program/threads/integration.json` only. Verification: fresh apply instructions; `pnpm install --frozen-lockfile`; `openspec validate --all --strict --no-interactive`; safety; typecheck; build; all unit/contract/integration tests; seven package exports; Playwright including streamed journey repeat; plugin package twice; temp install/fresh process smoke; exact seven/public-phase/PD-005/17+12+4/no-extra-chromakey-role/source/ZIP/retry/exact-stream-route/first-started/request-ID/terminal-before-EOF/no-sentinel/registration-plus-five-minutes-or-session-expiry-whichever-earlier/object-URL-cleanup/normal-and-near-expiry/exact-boundary/shutdown/session/security/native/dependency/scope audits; `git diff --check`; clean Git. If failure requires code, return to its owning task and repeat verification/commit sequence.

## 7. User-approved real-relay acceptance gate

- [ ] 7.1 `[lane=integration; owner=main agent; depends=6.3]` Create the approval-gated real-relay acceptance harness/checklist and redacted evidence validator for text generation, two references, direct edit, mask edit, partial batch, single-identity bounded transparency, capability evidence, request/cost counts, and shared Codex/Studio Library results. Allowed implementation files: `scripts/run-real-relay-acceptance.mjs`, `docs/acceptance/routego-image-real-relay.md`, `packages/integration/test/acceptance-harness.test.ts`. Forbidden: executing a real request, storing keys/auth/user images/provider bodies/output images, automatic endpoint derivation/probing, release/install actions. Verification before implementation commit: fresh apply instructions; harness refuses missing/expired/narrow approval and budget overflow; synthetic offline dry run; redaction/evidence schema; exact request matrix; chromakey success/failure records one artifact identity per slot, no extra role and 33-bound evidence; stop/cancel behavior; safety; diff check; scope review.
- [ ] 7.2 `[lane=integration; owner=main agent; depends=7.1; approval=user-explicit-current]` After the user explicitly approves the exact provider/profile, credential use, synthetic inputs, potential charges, request classes/count/budget, and evidence location, execute only the approved real-relay matrix and record redacted results. Allowed repository files after execution: `docs/acceptance/routego-image-real-relay-result.md`, this change's `tasks.md`, and `.codex/routego-program/threads/integration.json`; generated images and sensitive/raw evidence remain outside the repository in the approved new data roots. Verification: text, two-reference, direct-edit, mask-slot-zero, partial-batch, transparency with one output identity/no extra role/33 bound and provider-original fallback on chromakey failure; actual transport/shape/requests/cost risk; four-state/transient evidence; Codex/Studio same asset/source graph; no endpoint guessing/replay/secret/path/image payload; clean Git. If approval is missing/withdrawn, budget reached, or any required case cannot run, leave this task incomplete and do not claim acceptance.

## 8. User-approved release and rollback gate

- [ ] 8.1 `[lane=integration; owner=main agent; depends=7.1]` Read and use `plugin-creator` to prepare (but not execute) cachebuster/reinstall, staged atomic replacement, backup/archive, fresh-task post-install verification, rollback, hash/evidence, and legacy-preservation scripts/checklist. Allowed implementation files: `scripts/stage-routego-release.mjs`, `scripts/rollback-routego-release.mjs`, `docs/release/routego-image-1.0.md`, `packages/integration/test/release-workflow.test.ts`. Forbidden: changing marketplace/install state, deployment/publication, deleting/renaming the live plugin, reading/migrating legacy data, real release execution. Verification before implementation commit: fresh apply instructions; dry-run in temporary directories only; prove the task needs no credential, real-relay result, installation target mutation, or external authority; partial-write/switch/failure/rollback/fresh-task simulations; accepted hash enforcement; legacy immutability; redaction; plugin-creator dry validation; safety; diff check; scope review.
- [ ] 8.2 `[lane=integration; owner=main agent; depends=7.2,8.1; approval=user-explicit-current]` After the user explicitly approves the exact staged artifact/hash, local install target, marketplace/cachebuster action, downtime/atomic switch, backup location, rollback, and any deployment/publication scope, execute only that approved release scope, verify in a fresh Codex task, and restore the archived plugin automatically on failure. Allowed repository files after execution: `docs/release/routego-image-1.0-result.md`, this change's `tasks.md`, and `.codex/routego-program/threads/integration.json`; external installed/marketplace changes are limited to the approval. Verification: accepted artifact hash/version, completed real-relay acceptance, plugin discovery, Skill, exact-seven MCP, Studio/API/resource/stream, new data root, approved real-relay smoke if included, old plugin archive, rollback drill/result, legacy hashes/timestamps, no secret/image evidence, clean Git. Missing or narrower approval leaves the task incomplete and performs no broader action.

## 9. Final conformance and delivery

- [ ] 9.1 `[lane=integration; owner=main agent; depends=6.3,7.2,8.2]` Run the final complete Routego Image 1.0 gate, re-read proposal/design/all eleven delta specs/tasks, PD-002/003/004/005, all 18 main specs, all three archived functional changes, plan, AGENTS, program and Integration state; verify every implementation/external task has evidence and commits; run strict OpenSpec, safety, full typecheck/build/tests/exports/browser/package/install/real-relay/release/rollback audits, exact seven/public-phase freeze, cross-platform CI results, diff scope, `git diff --check`, and final Git cleanliness. If any failure requires code or external action, return to the owning task and repeat its verification/commit sequence. After all gates actually pass, update only allowed task/thread delivery state, record the full delivery SHA, and send `[INTEGRATION_COMPLETE]` to Controller with tasks, files, verification, approvals, blockers, residual risks, release/rollback state, and recommended spec sync/archive. No task may be marked complete merely because an approval was not granted.

> Checkbox ownership: only the Integration apply-owner updates this file. For every implementation task, run the listed verification, inspect the exact allowed-file scope, commit the implementation, record its full SHA in the task line, and only then mark the checkbox complete in a follow-up OpenSpec state commit. Proposal-only activation does not authorize any task execution. Real-relay and release tasks require fresh explicit user approval in addition to later Controller apply authorization.
